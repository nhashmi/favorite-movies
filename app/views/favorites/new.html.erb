<h3><%= link_to 'Signout', destroy_user_session_path, :method => :delete %></h3>

<%= form_for ([current_user, current_user.favorites.build]) do |f| %>
<%= f.label :movie %>
<%= f.text_field :movie %>
<%= f.submit "search" %>
<% end %>
